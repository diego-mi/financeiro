<h2>Lançar</h2>

<?php
/**
 * @var $form \Zend\Form\Form
 */
$form = $this->form;
$formLabel = $this->plugin('formLabel');

$this->formElementErrors()
    ->setMessageOpenFormat('<p class="text-danger">')
    ->setMessageCloseString('</p>')
    ->render($form);

$form->setAttribute('action',
    $this->url('lancamentos/default', array('controller' => 'lancamentos', 'action' => 'inserir')));

$form->prepare();

echo $this->form()->openTag($form);

?>
<div class="col-lg-12">
    <?php

    foreach ($form as $element) { ?>
        <div class="form-group">
            <?php
            if ($element->getAttribute('type') != 'submit') {
                echo ('<label for="' . $element->getAttribute('id') .'">'. $element->getLabel() . '</label>');
                echo($this->formElement($element));
            } else {
                echo($this->formInput($element));
            }
            echo($this->formElementErrors($element));
            ?>
        </div>
        <?php
    }
    ?>
</div>
<?php

echo $this->form()->closeTag();
?>

<style type="text/css">
    input, textarea, label, select {
        display: block;
    }
</style>

